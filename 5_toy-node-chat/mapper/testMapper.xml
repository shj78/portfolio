<?xml version="1.0" encoding="UTF-8">
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="testMapper">
<!--
    SELECT 의 경우
    <select namespace = "testBasic">
        SELECT
            *
        FROM
            USERS
        WHERE 
            USER_ID=#{user_id}    

    INSERT 

 -->
</mapper>